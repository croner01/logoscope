[pytest]
# pytest 配置文件

testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --tb=short
    --asyncio-mode=auto

asyncio_mode = auto

markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试
